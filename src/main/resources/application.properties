

spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update

springdoc.version=1.0
springdoc.swagger-ui.path=/api/users/swagger

#smtp 서버 호스트
spring.mail.host=${SPRING_MAIL_HOST}
#smtp 서버 포트
spring.mail.port=${SPRING_MAIL_PORT}
#메일 발신자 (test@gmail.com이면 test가 해당됨)
spring.mail.username=${SPRING_MAIL_USERNAME}
#메일 서버 비밀번호
spring.mail.password=${SPRING_MAIL_PASSWORD}
#사용자 인증 시도 여부 (기본값 false)
spring.mail.properties.mail.smtp.auth=true
#메일 전송 시간 초과 시간 설정 (기본값 무한대)
spring.mail.properties.mail.smtp.timeout=${SPRING_MAIL_PROPS_MAIL_SMTP_TIMEOUT}
#메일 전송 시 TLS 설정 (기본값 false)
spring.mail.properties.mail.smtp.starttls.enable=true
